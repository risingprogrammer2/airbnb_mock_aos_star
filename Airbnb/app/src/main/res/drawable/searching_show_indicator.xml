<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/input_profile_btn_continue">
        <shape
            android:shape="oval">
            <!-- 테두리 내부 색상 설정 -->
            <solid
                android:color="@color/white"/>

            <!-- 테두리 내부 여백 설정 -->
            <padding/>

            <!-- 테두리 설정: 두께, 색상 -->
            <stroke />

            <corners/>
        </shape>
    </item>
</layer-list>
